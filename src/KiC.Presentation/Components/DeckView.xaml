<UserControl x:Class="KiC.Presentation.Components.DeckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KiC.Presentation.Components"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="50">
    <Rectangle 
        Height="70"
        Width="50"
        >
        <Rectangle.Fill>
            <ImageBrush ImageSource="{Binding ImageSource}" />
        </Rectangle.Fill>
        <Rectangle.InputBindings>
            <MouseBinding Gesture="LeftClick" Command="{Binding OnDrawCardEvent}"/>
        </Rectangle.InputBindings>
    </Rectangle>
</UserControl>
